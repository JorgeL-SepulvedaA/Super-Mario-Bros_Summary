import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Super Mario Bros (Summary)',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
